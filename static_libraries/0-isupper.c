#include "main.h"

/**
 * _isupper - make the sum of 1 number
 * @c: First operand
 *
 * Return: the sum of the two parameters
 */

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
		return (1);
	return (0);
}
