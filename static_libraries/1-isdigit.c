#include "main.h"

/**
 * _isdigit - make the sum of 1 number
 * @c: First operand
 *
 * Return: the sum of the two parameters
 */

int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
		return (1);
	return (0);
}
