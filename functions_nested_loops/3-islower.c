#include "main.h"

/**
 * _islower - make the sum of 1 number
 * @c: First operand
 *
 * Return: the sum of the two parameters
 */

int _islower(int c)
{
	if (c >= 97 && c <= 122)
		return (1);
	return (0);
}
